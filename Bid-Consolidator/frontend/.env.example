# Local development needs nothing: Vite proxies /api and /ws to localhost:4000.
# On Vercel, set:
VITE_API_URL=https://bid-consolidator-api.onrender.com/api
# Optional: override the WebSocket URL (derived from VITE_API_URL by default).
# VITE_WS_URL=wss://bid-consolidator-api.onrender.com/ws
